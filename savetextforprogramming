  event.when(
            (index) => emit(state) ,// Не знаю, что это делает, но компик ругается
          initialize: () => emit(state),
          newNote: _newNote(event, emit),
          editNote: _editNote(event, emit),
          deleteNote: _deleteNote(event, emit),
        );